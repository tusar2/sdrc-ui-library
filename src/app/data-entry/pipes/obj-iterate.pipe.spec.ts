import { ObjIteratePipe } from './obj-iterate.pipe';

describe('ObjIteratePipe', () => {
  it('create an instance', () => {
    const pipe = new ObjIteratePipe();
    expect(pipe).toBeTruthy();
  });
});
